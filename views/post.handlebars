    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/project/{{post.id}}" class="text-dark">{{post.title}}</a></h4>
      </div>
      <p>{{post.content}}</p>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
